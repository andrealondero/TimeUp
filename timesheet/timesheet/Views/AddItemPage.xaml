<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:timesheet"
             x:Class="timesheet.Views.AddItemPage"
             NavigationPage.HasNavigationBar="True"
             Title="NEW TIMESHEET">

    <ScrollView>

        <StackLayout Spacing="20" Padding="15">

            <Entry x:Name="activateDate" IsVisible="True" Placeholder="Select a Date" FontSize="Medium" PlaceholderColor="#910C07" />
            <Label x:Name="dateLabel" Text="{Binding NullableDate, Source={x:Reference datepicker}, StringFormat='{0:yyyy-MM-dd}'}" FontSize="Medium" />
            <!--Label Text="Select Date" TextColor="#2D8183" FontSize="Medium" /-->
            <local:NullableDatePicker x:Name="datepicker" IsVisible="False" IsEnabled="False" NullableDate="{Binding Item.Date}" MinimumDate="2019-01-01" MaximumDate="{x:Static system:DateTime.Now}" DateSelected="Datepicker_DateSelected"/>
            <Label Text="Worked Hours" TextColor="#2D8183" FontSize="Medium" />
            <Label x:Name="hoursLabel" FontSize="Medium" />
            <Stepper x:Name="stephours" Minimum="0" Maximum="12" Increment="1" Value="{Binding Hours}" ValueChanged="Stepper_ValueChanged"/>
            <Label Text="Description" TextColor="#2D8183" FontSize="Medium" />
            <Editor x:Name="descriptioneditor" Text="{Binding Description, Mode=TwoWay}" Placeholder="your activities here" PlaceholderColor="#910C07" HeightRequest="100" MaxLength="120" FontSize="Medium" Margin="0" />

            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" Margin="10,0,10,0">
                <Button Text="SAVE" Clicked="OnSaveClicked" BackgroundColor="#83D3D4" TextColor="White" BorderColor="White" BorderWidth="0.5" BorderRadius="10" HeightRequest="80" WidthRequest="80" Margin="0,0,0,20" HorizontalOptions="Center" />
                <Button Text="BACK" Clicked="OnCancelClicked" BackgroundColor="#83D3D4" TextColor="White" BorderColor="White" BorderWidth="0.5" BorderRadius="10" HeightRequest="80" WidthRequest="80" Margin="0,0,0,20" HorizontalOptions="Center" />
            </StackLayout>

        </StackLayout>

    </ScrollView>
    
</ContentPage>